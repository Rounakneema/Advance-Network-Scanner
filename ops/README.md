# DevOps Resources

This directory is reserved for Infrastructure-as-Code (IaC) and deployment configurations.

## Future Plans
- **Docker**: `Dockerfile` and `docker-compose.yml` for containerized scanning nodes.
- **K8s**: Helm charts for deploying Revealr as a CronJob or Service.
- **Terraform**: Scripts to provision scanning infrastructure on AWS/GCP.
